import LoadingSpinner from "../../assets/loading.gif";

const Spinner = () => {
  return (
    <div>
      <img src={LoadingSpinner} width="50px" height="50px" />
    </div>
  );
};

export default Spinner;
